config:
  target: "https://bj3ibqqj3h.execute-api.eu-west-3.amazonaws.com/Prod"
  phases:
    - duration: 60
      arrivalRate: 600

scenarios:
  - flow:
      - get:
          url: "/Prod/ping"